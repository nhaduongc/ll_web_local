---
import * as Use from '../Switch'

const {
  value
} = Astro.props

const exists = value ? "exists" : null
---

<Use.Switch value={exists}>
  <Use.Case slot={"exists"}>
    <slot />
  </Use.Case>
</Use.Switch>