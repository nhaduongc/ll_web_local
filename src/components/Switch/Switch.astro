---
const { value } = Astro.props;
const key = value;

let html: string;

if (Astro.slots.has(key)) {
	html = await Astro.slots.render(key);
} else if (Astro.slots.has('default')) {
	html = await Astro.slots.render('default');
}
---

<Fragment set:html={html} />